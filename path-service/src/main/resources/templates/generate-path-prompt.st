Generate a detailed career learning path for the goal: {goal}.

Format your response exactly as per the following JSON structure:
{format}

Strictly follow these rules:
- Output only valid JSON.
- Use double quotes (`"`) around all string values.
- Use commas between each key-value pair inside JSON objects.
- Use camelCase for all keys.
- Do not add comments, explanations, or markdown — only pure JSON.
- Ensure each step contains: `stepNumber` (integer), `title` (string), `description` (string), `duration` (string).

Keep all descriptions concise and durations realistic.
